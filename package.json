{
  "name": "vite-deneme",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/vite-deneme/",
    "preview": "vite preview",
    "golive": "npm run build && npm run preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "simplelightbox": "^2.14.3"
  }
}
